import { createSlice } from "@reduxjs/toolkit";

export const userSlice = createSlice({
    //name of this slice
    name: "user",
    // initial state of the reducer
    initialState: {
        value: []
    },
    //reducers for thsi specific slice, can be multiple slices and reducers within the slices
    reducers: {
        
    }
})